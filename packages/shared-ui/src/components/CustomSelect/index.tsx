/*
 * CustomSelect — backwards-compat re-export.
 * New code should import from 'sharedUi/AppSelect' instead.
 */
export { AppSelect as default, AppSelect, type AppSelectProps, type SelectOption } from '../AppSelect/index';
